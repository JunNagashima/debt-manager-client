export enum AdvanceType {
  NORMAL = 'NORMAL',
  ADJUSTMENT = 'ADJUSTMENT',
}

export enum RepaymentSource {
  MANUAL = 'MANUAL',
  OFFSET = 'OFFSET',
}

export enum RequestStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
  CANCELED = 'CANCELED',
}
